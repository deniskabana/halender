const hello = 'World';

new Array(5).fill(hello).forEach((item, index) => {
  console.log(`${index + 1}: ${item}`);
});

doesNotExist.nope(`I'm never imported`);

export class Widget extends React.Component {
  brit() {
    this.something = 'hey';
    return false;
  }
}

const function1 = function() {};
const function2 = () => {};
console.log(joe, brit);
